[package]
name = "aegis_graph_core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "AEGIS Dependency Graph Kernel — DAG operations (toposort, critical path, cycle detection)"
publish = false

[dependencies]
aegis_ffi_common = { path = "../aegis_ffi_common" }

[dev-dependencies]
# no extra dev deps needed
